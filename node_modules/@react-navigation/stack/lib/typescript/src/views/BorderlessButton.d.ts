import * as React from 'react';
import { BaseButtonProperties } from 'react-native-gesture-handler';
declare type Props = BaseButtonProperties & {
    pressOpacity: number;
};
export default class BorderlessButton extends React.Component<Props> {
    static defaultProps: {
        activeOpacity: number;
        borderless: boolean;
    };
    private opacity;
    private handleActiveStateChange;
    render(): JSX.Element;
}
export {};
